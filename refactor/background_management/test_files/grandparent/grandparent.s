predict(grandparent(+num, -class), classification).

talking(3).
use_packs(0).
resume(off).
sampling_strategy(none).
heuristic(gain).

use_packs(0).
minfreq(0.2).

typed_language(yes).
type(divides(num, primeconst, num)).

rmode(1: divides(+N, #[2,3,5,7], -M)).
